import React from 'react';
import styled from 'styled-components';
import RegisterComplete from 'app.feature/register/RegisterComplete';

const PageRegisterComplete = () => {
  return (
    <StyledWrapper>
      <RegisterComplete />
    </StyledWrapper>
  );
};

export default PageRegisterComplete;

const StyledWrapper = styled.div``;
